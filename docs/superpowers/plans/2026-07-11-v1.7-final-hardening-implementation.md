# v1.7 最终稳定性与证据闭环实施计划

> **执行规则：** 每个任务先建立可复现证据，再修改实现；完成 focused verification、规格符合性审查、代码质量审查和独立提交后，才能进入下一任务。

**目标：** 修复 Windows 全量 pytest 卡顿和 SSE 进度回调静默失败，在低风险职责边界内完成代码收口，并让同一 HEAD 通过远端完整 CI 与真实模型评测。

**设计依据：** `docs/superpowers/specs/2026-07-11-v1.7-final-hardening-design.md`

---

## Task 1：定位并修复 Windows 全量 pytest 卡顿

**调查与候选修改文件：**

- `backend/tests/conftest.py`
- 首个卡顿测试及其直接依赖
- `backend/app/db/connection.py`
- `backend/app/db/write_queue.py`
- `backend/app/agents/session_store.py`
- `backend/app/services/query_cache.py`
- `backend/app/services/prompt_registry.py`
- `backend/app/services/ab_test.py`
- 对应 focused test 文件

**步骤：**

- [ ] 记录 `pytest backend/tests --collect-only -vv` 和 `pytest backend/tests -vv -x` 的最后可见测试，设置有限诊断时间并检查残留 Python 进程。
- [ ] 使用逐文件/二分顺序运行确认是收集期副作用、测试顺序污染、数据库锁还是未退出线程。
- [ ] 新增能够证明资源未释放或顺序污染的失败回归测试。
- [ ] 在资源所有者处补齐 close/shutdown/reset；不跳过测试、不永久拆分 CI、不增加无限等待。
- [ ] 先运行 focused test，再运行 `pytest backend/tests -q`；完成后检查没有本次 pytest 残留进程。
- [ ] 运行 Ruff 和 Secret Scan。
- [ ] 完成规格符合性审查和代码质量审查。
- [ ] 提交 `fix: make backend test suite terminate reliably`。

**验收：** Windows 单命令全量测试完成并输出最终汇总；CI 命令仍保持单次全量执行和 75% 覆盖率门槛。

---

## Task 2：抽取并加固 SSE 进度通知边界

**修改文件：**

- 新增 `backend/app/agents/progress_notifier.py`
- `backend/app/agents/graph.py`
- 新增 `backend/tests/test_progress_notifier.py`
- 必要时调整 `backend/tests/test_agent_graph.py`

**步骤：**

- [ ] 先写同步成功、异步成功、awaitable 返回、缺失回调和失败脱敏测试。
- [ ] 新增独立同步/异步进度通知入口；关键错误边界添加中文注释。
- [ ] 失败日志只包含 stage 和异常类型，明确断言异常消息、查询内容和 Secret 不出现在 caplog。
- [ ] 保持回调异常不影响 Agent 主流程。
- [ ] `AgentGraph` 删除重复 try/except，只委托新模块；不改变任何节点和条件边。
- [ ] 运行 notifier、Graph、SSE/API focused tests和核心路径 Runner。
- [ ] 运行 Ruff、Secret Scan 和 `git diff --check`。
- [ ] 完成规格符合性审查和代码质量审查。
- [ ] 提交 `refactor: isolate agent progress notifications`。

**验收：** 进度旁路失败可观测且脱敏，Agent 查询结果和工作流拓扑不变。

---

## Task 3：维护性说明与本地冻结验证

**修改文件：**

- `docs/data_analyst_agent_开发文档_v_1_7.md`
- `docs/interview_guide.md`
- `backend/tests/test_project_docs_consistency.py`
- `logs/work-diary.md`（只暂存本轮对应 hunk）

**步骤：**

- [ ] 记录 v1.8 候选拆分边界：图节点编排、LLM transport/结构化解析、权限策略编译/SQL 改写。
- [ ] 明确触发条件是新业务需求或模块继续增长，不把技术债描述成当前功能缺陷。
- [ ] 同步 Windows 全量测试、进度通知和证据闭环的可审计说明。
- [ ] 运行文档一致性测试。
- [ ] 运行后端全量测试、Ruff、前端 lint/test/build、15 条核心路径、迁移/工作流契约和 Secret Scan。
- [ ] 对本轮全部差异依次完成规格符合性审查与代码质量审查。
- [ ] 只暂存本轮日志 hunk，保留教程任务工作树内容。
- [ ] 提交 `docs: record final hardening evidence`。

**验收：** 本地验证矩阵完整，文档不声称尚未执行的远端结果，工作树只剩用户原有教程日志改动。

---

## Task 4：推送分支并闭环当前 HEAD 的 GitHub CI

**远端操作：**

- 分支：`codex/v1.7-final-hardening`
- 工作流：`.github/workflows/ci.yml`

**步骤：**

- [ ] 推送分支并记录 HEAD SHA。
- [ ] 用该 SHA 查询 CI run，确认不是旧提交结果。
- [ ] 依次核对后端、PostgreSQL、前端、E2E、Docker、依赖审计和 Secret Scan jobs。
- [ ] 若失败，下载失败日志，回到对应 Task 修复、复核、提交并重新推送。
- [ ] 所有 jobs 成功后记录 run URL 和 SHA。

**验收：** 当前分支 HEAD 的完整 CI conclusion 为 `success`。

---

## Task 5：运行同 SHA 真实模型评测并核验证据包

**远端操作与证据：**

- 工作流：`.github/workflows/real-qwen-evaluation.yml`
- 脚本：`scripts/interview_evidence.py`
- 下载目录：`artifacts/real-llm-evaluation-<run_id>`

**步骤：**

- [ ] 基础 CI 全绿后，对当前分支触发真实模型工作流。
- [ ] 核对 workflow 使用的 HEAD SHA 与基础 CI、当前本地 HEAD 完全一致。
- [ ] 等待 smoke、完整评测、quality gate 和安全审计结束。
- [ ] 下载 artifact，检查 provider/model、脱敏 endpoint、case pack、UTC 时间和 HEAD SHA。
- [ ] 运行 `scripts/interview_evidence.py --run-id <run_id>` 生成证据清单。
- [ ] 若 Secret、额度或外部模型服务阻塞，保留 run URL 和失败 job，明确标记为外部阻塞，不复用旧报告。
- [ ] 如产生文档或日志更新，完成双重审查并独立提交后再次确保 SHA/证据关系准确。

**验收：** 当前 HEAD 有成功且字段完整的真实模型 artifact；若为不可由代码修复的外部阻塞，则提供可审计失败证据。

## 最终验收命令

```powershell
pytest backend/tests -q
python -m ruff check backend/app backend/evaluation database scripts backend/alembic
git ls-files -z | python scripts/check_secrets.py
npm run lint --prefix frontend
npm run test --prefix frontend
npm run build --prefix frontend
cd backend; python -m evaluation.core_path_runner
```

远端最终验收以当前 HEAD 的 GitHub CI 和 Real LLM workflow 为准，本地旧报告不得替代。

## 计划自审

- 设计中的五项目标均映射到任务、文件、命令和验收证据。
- 测试卡顿先复现再修复，禁止用拆分或跳过掩盖。
- AgentGraph 只抽取进度旁路，不改变 SQL Guard、权限和 Repair 工作流不变量。
- 本地、基础 CI、真实模型证据按依赖顺序执行，避免旧 SHA 证据混用。
- 教程日志改动明确隔离，提交范围可由 staged diff 审计。

