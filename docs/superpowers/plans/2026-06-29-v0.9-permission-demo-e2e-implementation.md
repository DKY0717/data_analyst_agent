# v0.9 Permission Demo E2E Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a stable Playwright E2E demo that proves the v0.8 role-switching permission workflow can be shown and verified without relying on real LLM calls.

**Architecture:** Use Playwright route mocks for auth, schema, standard query, and SSE query endpoints while exercising the real Vue UI. Keep backend permission code unchanged; this E2E is an interview/demo acceptance gate, not a replacement for backend pytest and evaluation suites.

**Tech Stack:** Playwright, Vue 3, Vite, Element Plus, Pinia, existing frontend dev server and existing FastAPI backend server from Playwright config.

---

## Scope Check

This plan covers one focused subsystem: v0.9 permission demo acceptance. It creates a single E2E spec, makes existing E2E assertions compatible with the added permission question group, updates README, and records progress in the work diary.

## File Structure

- Create: `frontend/e2e/permission-demo.spec.js`
  - Contains route mocks and the analyst/admin permission demo E2E.
- Modify: `frontend/e2e/app.spec.js`
  - Updates the example-question count assertion so v0.8's permission question group does not break the baseline page test.
- Modify: `README.md`
  - Adds the 30-second interview demo script and the targeted E2E command.
- Modify: `logs/work-diary.md`
  - Records v0.9 implementation and verification evidence.

---

## Task 1: Permission Demo E2E Spec

**Files:**
- Create: `frontend/e2e/permission-demo.spec.js`

- [ ] **Step 1: Write the failing E2E test**

Create `frontend/e2e/permission-demo.spec.js` with:

```js
import { test, expect } from '@playwright/test'

let currentRole = 'guest'

const schemaResponse = {
  code: 200,
  message: 'success',
  data: {
    tables: {
      orders: { columns: [{ name: 'order_id' }, { name: 'total_amount' }] },
      customers: { columns: [{ name: 'customer_name' }, { name: 'registered_at' }] },
    },
  },
}

function successResult(user, answer, rows = [['2024-01', 128000]]) {
  return {
    question: '',
    session_id: 'e2e-session',
    status: 'completed',
    answer,
    sql: 'SELECT 1',
    generated_sql: 'SELECT 1',
    optimized_sql: 'SELECT 1',
    columns: ['month', 'sales'],
    rows,
    is_sql_safe: true,
    execution_time_ms: 12,
    retry_count: 0,
    audit_report: {
      user_id: user.user_id,
      auth_method: user.auth_method,
      roles: user.roles,
      is_sql_safe: true,
      execution_success: true,
      limit_injected: false,
      blocked_rules: [],
      execution_metrics: { row_count: rows.length, total_latency_ms: 120, llm_call_count: 0 },
      events: [
        {
          stage: 'authorization',
          action: 'authorize_sql',
          status: 'success',
          message: `角色 ${user.roles[0]} 通过数据权限检查`,
          rule_id: null,
        },
      ],
    },
  }
}

function blockedResult(user) {
  return {
    question: '列出客户姓名和注册日期',
    session_id: 'e2e-session',
    status: 'blocked',
    answer: '请求已被数据权限策略阻断：analyst 不能访问 customers.customer_name。',
    sql: '',
    generated_sql: '',
    optimized_sql: '',
    columns: [],
    rows: [],
    is_sql_safe: true,
    execution_time_ms: 0,
    retry_count: 0,
    audit_report: {
      user_id: user.user_id,
      auth_method: user.auth_method,
      roles: user.roles,
      is_sql_safe: true,
      execution_success: false,
      limit_injected: false,
      blocked_rules: ['block_unauthorized_column:customers.customer_name'],
      execution_metrics: { row_count: 0, total_latency_ms: 30, llm_call_count: 0 },
      events: [
        {
          stage: 'authorization',
          action: 'authorize_sql',
          status: 'blocked',
          message: '角色 analyst 无权访问 customers.customer_name',
          rule_id: 'block_unauthorized_column',
        },
      ],
    },
  }
}

function userForRole(role) {
  if (role === 'admin') return { user_id: 'demo:admin', auth_method: 'jwt', roles: ['admin'] }
  if (role === 'analyst') return { user_id: 'demo:analyst', auth_method: 'jwt', roles: ['analyst'] }
  return { user_id: 'anonymous', auth_method: 'none', roles: ['guest'] }
}

function toSse(result) {
  return [
    'data: {"type":"progress","stage":"权限检查","progress":60}\\n\\n',
    `data: ${JSON.stringify({ type: 'result', data: result })}\\n\\n`,
    'data: [DONE]\\n\\n',
  ].join('')
}

async function mockBackend(page) {
  currentRole = 'guest'

  await page.route('**/api/schema', async route => {
    await route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify(schemaResponse) })
  })

  await page.route('**/api/auth/demo-login', async route => {
    const body = route.request().postDataJSON()
    currentRole = body.role
    const user = userForRole(currentRole)
    await route.fulfill({
      status: 200,
      contentType: 'application/json',
      body: JSON.stringify({
        code: 200,
        message: 'demo login success',
        data: { access_token: `mock-token-${currentRole}`, token_type: 'bearer', expires_in: 86400, user },
      }),
    })
  })

  await page.route('**/api/auth/me', async route => {
    await route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify(userForRole(currentRole)) })
  })

  await page.route('**/api/chat/query/stream', async route => {
    const body = route.request().postDataJSON()
    const user = userForRole(currentRole)
    const result = body.question.includes('客户姓名') && currentRole === 'analyst'
      ? blockedResult(user)
      : successResult(
          user,
          currentRole === 'admin' && body.question.includes('客户姓名')
            ? '管理员已成功查询客户姓名和注册日期。'
            : '2024 年每个月销售额已统计完成。',
          currentRole === 'admin' && body.question.includes('客户姓名')
            ? [['张三', '2024-01-03']]
            : [['2024-01', 128000], ['2024-02', 136000]],
        )

    await route.fulfill({ status: 200, contentType: 'text/event-stream', body: toSse(result) })
  })

  await page.route('**/api/chat/query', async route => {
    const body = route.request().postDataJSON()
    const user = userForRole(currentRole)
    const result = body.question.includes('客户姓名') && currentRole === 'analyst'
      ? blockedResult(user)
      : successResult(user, '管理员已成功查询客户姓名和注册日期。', [['张三', '2024-01-03']])

    await route.fulfill({
      status: 200,
      contentType: 'application/json',
      body: JSON.stringify({ code: 200, message: 'success', data: result }),
    })
  })
}

async function submitQuestion(page, question) {
  await page.locator('.query-card textarea').fill(question)
  await page.locator('.submit-button').click()
}

test.describe('Permission Demo E2E', () => {
  test.beforeEach(async ({ page }) => {
    await mockBackend(page)
    await page.goto('/')
    await page.waitForLoadState('networkidle')
  })

  test('analyst blocked flow and admin success flow are demonstrable', async ({ page }) => {
    await expect(page.locator('.auth-bar')).toContainText('未认证')

    await page.locator('[data-test="role-analyst"]').click()
    await expect(page.locator('.auth-bar')).toContainText('demo:analyst')
    await expect(page.locator('.auth-bar')).toContainText('jwt')
    await expect(page.locator('.auth-bar')).toContainText('analyst')

    await submitQuestion(page, '统计 2024 年每个月的销售额')
    await expect(page.locator('.answer-panel')).toContainText('2024 年每个月销售额已统计完成。')

    await submitQuestion(page, '列出客户姓名和注册日期')
    await expect(page.locator('.answer-panel')).toContainText('请求已被数据权限策略阻断')
    await expect(page.locator('.audit-panel')).toContainText('demo:analyst')
    await expect(page.locator('.audit-panel')).toContainText('jwt')
    await expect(page.locator('.audit-panel')).toContainText('analyst')
    await expect(page.locator('.audit-panel')).toContainText('authorization')
    await expect(page.locator('.audit-panel')).toContainText('block_unauthorized_column')
    await expect(page.locator('.audit-panel')).toContainText('block_unauthorized_column:customers.customer_name')

    await page.locator('[data-test="role-admin"]').click()
    await expect(page.locator('.auth-bar')).toContainText('demo:admin')

    await submitQuestion(page, '列出客户姓名和注册日期')
    await expect(page.locator('.answer-panel')).toContainText('管理员已成功查询客户姓名和注册日期。')
    await expect(page.locator('.audit-panel')).toContainText('demo:admin')
    await expect(page.locator('.audit-panel')).toContainText('authorization')
  })
})
```

- [ ] **Step 2: Run E2E and verify RED**

Run:

```bash
cd frontend
npm run test:e2e -- permission-demo.spec.js
```

Expected: FAIL if the current UI/test selectors or E2E environment do not yet support the new permission demo flow. If it passes immediately because v0.8 already supports the flow, record that the test is a characterization E2E and continue to Task 2.

- [ ] **Step 3: Fix only what the E2E proves is missing**

If the test fails due to missing stable selectors, add these minimal attributes:

```vue
<!-- frontend/src/components/AnswerPanel.vue -->
<section class="panel answer-panel" data-test="answer-panel">
```

```vue
<!-- frontend/src/components/AuditPanel.vue -->
<section class="panel detail-panel audit-panel" data-test="audit-panel">
```

Then update the spec locators from class selectors to `page.getByTestId('answer-panel')` and `page.getByTestId('audit-panel')` only if needed.

- [ ] **Step 4: Run E2E and verify GREEN**

Run:

```bash
cd frontend
npm run test:e2e -- permission-demo.spec.js
```

Expected: PASS.

- [ ] **Step 5: Commit E2E slice**

Run:

```bash
git add frontend/e2e/permission-demo.spec.js frontend/src/components/AnswerPanel.vue frontend/src/components/AuditPanel.vue
git commit -m "test: add permission demo e2e"
```

If `AnswerPanel.vue` or `AuditPanel.vue` were not changed, omit them from `git add`.

---

## Task 2: Existing E2E Compatibility

**Files:**
- Modify: `frontend/e2e/app.spec.js`

- [ ] **Step 1: Run the existing E2E suite and identify current failure**

Run:

```bash
cd frontend
npm run test:e2e
```

Expected: The existing "示例问题展示 12 条" assertion may fail because v0.8 added another permission demo question group.

- [ ] **Step 2: Update the example-question assertion**

Modify the test in `frontend/e2e/app.spec.js`:

```js
  test('普通示例问题展示 12 条，权限演示问题展示 4 条', async ({ page }) => {
    const panels = page.locator('.left-column .compact-panel')
    await expect(panels.nth(0).locator('.question-chip')).toHaveCount(12)
    await expect(panels.nth(1).locator('.question-chip')).toHaveCount(4)
  })
```

- [ ] **Step 3: Run existing E2E and verify GREEN**

Run:

```bash
cd frontend
npm run test:e2e
```

Expected: PASS.

- [ ] **Step 4: Commit E2E compatibility slice**

Run:

```bash
git add frontend/e2e/app.spec.js
git commit -m "test: align e2e with permission questions"
```

---

## Task 3: README Demo Script

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Add 30-second interview demo script**

In `README.md`, under `### v0.8 权限演示工作台`, add:

```markdown
#### 30 秒面试演示路径

1. 在顶部身份条点击 `Analyst`，确认当前身份显示为 `demo:analyst`。
2. 提交 `统计 2024 年每个月的销售额`，展示分析结果正常返回。
3. 提交 `列出客户姓名和注册日期`，展示请求被数据权限策略阻断。
4. 打开右侧安全审计，指出 `demo:analyst`、`authorization blocked` 和 `block_unauthorized_column`。
5. 切换为 `Admin`，再次提交同一客户姓名问题，展示管理员查询成功。

这条演示路径说明：Agent 不只会生成 SQL，还能在最终 SQL 执行前做角色级字段权限校验，并把阻断证据写入审计报告。
```

- [ ] **Step 2: Add targeted E2E command**

In the test section, add:

```bash
# 权限演示 E2E（Mock 后端响应，不依赖真实 LLM）
cd frontend && npm run test:e2e -- permission-demo.spec.js
```

- [ ] **Step 3: Run docs hygiene checks**

Run:

```bash
rg -n "TB[D]|TO[D]O|XX[X]|待[补]|以后再[说]" README.md docs/superpowers/plans docs/superpowers/specs logs/work-diary.md
git diff --check
```

Expected: no unexpected hits and `git diff --check` exits 0.

- [ ] **Step 4: Commit README slice**

Run:

```bash
git add README.md
git commit -m "docs: add permission demo interview script"
```

---

## Task 4: Diary and Final Verification

**Files:**
- Modify: `logs/work-diary.md`

- [ ] **Step 1: Update work diary**

Append:

```markdown
## 2026-06-29 — v0.9 权限演示 E2E 闭环实现

### 完成的工作

- 新增 Mock 后端响应的 Playwright 权限演示 E2E。
- 修正基础 E2E 对普通示例问题和权限演示问题的数量断言。
- README 新增 30 秒面试演示路径和定向 E2E 命令。

### 当前进度

- ✅ v0.9 权限演示验收闭环完成。
- ✅ E2E 不依赖真实 Qwen/MiMo API。

### 最终验证

- `npm run test:e2e -- permission-demo.spec.js`
- `npm run test:e2e`
- `npm run test`
- `npm run build`
- Secret Scan
- `git diff --check`

### 下一步

- 继续 v1.0：权限策略外部化和行级权限设计。
```

- [ ] **Step 2: Run final frontend verification**

Run:

```bash
cd frontend
npm run test:e2e -- permission-demo.spec.js
npm run test:e2e
npm run test
npm run build
```

Expected: all commands exit 0. Existing chunk-size warning during build is acceptable.

- [ ] **Step 3: Run repository hygiene checks**

Run:

```bash
rg -n "TB[D]|TO[D]O|XX[X]|待[补]|以后再[说]" README.md docs/superpowers/plans docs/superpowers/specs logs/work-diary.md backend/app frontend/src backend/tests frontend/tests frontend/e2e
git diff --check
git ls-files -z | python scripts/check_secrets.py
```

Expected: no placeholder hits, diff check passes, Secret Scan passes.

- [ ] **Step 4: Commit diary slice**

Run:

```bash
git add logs/work-diary.md
git commit -m "docs: record v0.9 permission demo e2e"
```

---

## Acceptance Checklist

- [ ] `frontend/e2e/permission-demo.spec.js` exists.
- [ ] The new E2E mocks auth, schema, standard query, and SSE query endpoints.
- [ ] The new E2E proves analyst login, analyst successful sales query, analyst blocked customer-name query, and admin successful customer-name query.
- [ ] The new E2E asserts AuditPanel identity and authorization evidence.
- [ ] Existing E2E suite passes after v0.8 permission question group.
- [ ] README contains a 30-second interview demo path.
- [ ] README includes targeted permission-demo E2E command.
- [ ] No backend permission logic is changed.
- [ ] Final verification commands pass.
- [ ] Changes are committed in focused commits.

## Self-Review

**Spec coverage:** The plan maps each v0.9 goal to a task: Task 1 covers the permission demo E2E and mocks, Task 2 keeps existing E2E compatible, Task 3 covers README, and Task 4 covers final verification and diary.

**Placeholder scan:** The plan avoids unresolved placeholder markers and uses bracketed regex forms in verification commands to avoid self-triggering scans.

**Type consistency:** Mock response shapes match current frontend expectations: `SuccessResponse.data` for normal query, raw result data in SSE `event.data`, and `audit_report` with `user_id`, `auth_method`, `roles`, `blocked_rules`, and `events`.
