# v1.7 真实模型质量门禁修复实施计划

> **执行规则：** 每个任务先补失败回归测试，再实现最小修复；focused verification、规格符合性审查、代码质量审查全部通过后独立提交。

**目标：** 修复真实 Qwen 评测暴露的权限查询作用域误判、地域维度过度展开和评测错误不可见问题，并让新 HEAD 通过 100% 强制质量门禁。

**设计依据：** `docs/superpowers/specs/2026-07-15-v1.7-real-model-quality-remediation-design.md`

---

## Task 1：查询作用域感知的数据权限解析

**修改文件：**

- `backend/app/security/data_permission.py`
- `backend/tests/test_data_permission_guard.py`
- 必要时 `backend/tests/test_permission_execution_integration.py`

**步骤：**

- [ ] 用真实评测生成的多层 CTE 和派生子查询 SQL 新增失败回归测试。
- [ ] 增加 Analyst 通过 CTE、派生子查询读取 `customers.customer_name` 的越权测试。
- [ ] 增加无法唯一归属字段继续 `block_ambiguous_column` 的回归断言。
- [ ] 使用 SQLGlot scope 遍历查询层，区分物理 `Table` 和派生 `Scope`。
- [ ] 在当前 scope 内解析有限定列、裸列和星号；相关外层字段沿父 scope 解析。
- [ ] 只跳过已经在子 scope 检查过的派生输出，不能跳过底层物理字段。
- [ ] 保持表级授权、字段级授权、行过滤注入和审计格式不变。
- [ ] 运行权限 Guard、权限集成、Agent Graph focused tests。
- [ ] 运行 Ruff、Secret Scan 和 `git diff --check`。
- [ ] 依次完成规格符合性审查与代码质量审查。
- [ ] 提交 `fix: resolve permission columns by query scope`。

**验收：** 两条真实失败 SQL 对管理员通过；Analyst 的敏感字段穿透继续被确定性阻断；既有权限测试全部通过。

---

## Task 2：地域语义维度最小化

**修改文件：**

- `backend/app/semantic/ecommerce_metrics.yaml`
- `backend/tests/test_semantic_loader.py`
- `backend/tests/test_schema_grounding_precision.py`
- `backend/tests/test_sql_generator.py`

**步骤：**

- [ ] 先新增“地区仅 region_name”“省份仅 province”“城市仅 city”的失败测试。
- [ ] 为三个维度定义稳定且互不重复的 candidate ID。
- [ ] 复用相同的 customers/regions JOIN 路由，不复制无关业务逻辑。
- [ ] 断言“按地区”的 Grounding expression 和 SQL prompt 不包含 province/city。
- [ ] 断言省份和城市别名仍能准确命中对应维度。
- [ ] 运行 semantic loader、Grounding、intent integration、SQL Generator focused tests。
- [ ] 运行 Ruff、Secret Scan 和 `git diff --check`。
- [ ] 依次完成规格符合性审查与代码质量审查。
- [ ] 提交 `fix: separate geographic semantic dimensions`。

**验收：** “地区”严格映射 `regions.region_name`，省份和城市仍具备独立可解释 Grounding，既有意图路由不回归。

---

## Task 3：NL2SQL 权限阻断可观测性

**修改文件：**

- `backend/evaluation/evaluator.py`
- `backend/tests/test_evaluator.py`
- 必要时 `backend/evaluation/report_writer.py`
- 必要时对应报告测试

**步骤：**

- [ ] 新增 permission denied state 的评测失败测试，证明当前 `blocked_stage` 和 `error` 信息缺失。
- [ ] 将 SQL Guard 通过且权限失败的阶段记录为 `permission_guard`。
- [ ] 输出 `permission_allowed`、脱敏 blocked rule 和 `permission_error`。
- [ ] 保持 safe case 必须执行成功、unsafe case 只接受 Intent/SQL Guard 阻断的指标定义。
- [ ] 确认报告不输出策略表达式、凭据或原始身份数据。
- [ ] 运行 evaluator、report writer、quality gate、安全审计 focused tests。
- [ ] 运行 Ruff、Secret Scan 和 `git diff --check`。
- [ ] 依次完成规格符合性审查与代码质量审查。
- [ ] 提交 `fix: expose permission failures in nl2sql evaluation`。

**验收：** 权限误阻断在 JSON/Markdown 中有明确阶段、规则和脱敏原因，不再出现无原因失败；指标分母和安全定义不变。

---

## Task 4：本地冻结验证与证据更新

**验证：**

- [ ] 运行三个任务的全部 focused tests。
- [ ] 运行后端全量 branch coverage 命令，门槛保持 75%。
- [ ] 运行 Ruff、Secret Scan、`git diff --check` 和 15 条核心路径。
- [ ] 运行文档一致性和工作流契约测试。
- [ ] 对本轮全部提交依次做最终规格符合性审查与代码质量审查。
- [ ] 仅更新并暂存本轮 work diary hunk，不覆盖用户原有教程日志改动。
- [ ] 提交本地证据更新。

**验收：** 本地所有门禁通过，工作树只保留用户原有日志改动和可安全清理的临时 artifact。

---

## Task 5：推送并闭环远端证据

**步骤：**

- [ ] 推送 `codex/v1.7-final-hardening` 并记录新 HEAD。
- [ ] 等待该 SHA 的完整基础 CI 全绿。
- [ ] 对相同 SHA 触发 Qwen `qwen-plus` 强制阈值真实模型工作流。
- [ ] 核对 smoke、NL2SQL、SQL Repair、结果正确性、Grounding、权限、Quality Gate 和严格安全审计。
- [ ] 下载 artifact 并核对 SHA、provider、model、endpoint、case pack 和 UTC 时间。
- [ ] 若模型仍产生随机质量偏差，依据新 artifact 判断是否为新的可复现实现缺陷；不得降低阈值或复用旧报告。
- [ ] 安全清理 workspace 内 `.codex-artifacts` 临时目录。

**验收：** 新 HEAD 的基础 CI、100% quality gate 和严格安全审计成功；证据 artifact 与提交 SHA 一致。

## 最终验收命令

```powershell
pytest backend/tests -q --cov=backend/app --cov=backend/evaluation --cov-config=pyproject.toml --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=75
python -m ruff check backend/app backend/evaluation database scripts backend/alembic
git ls-files -z | python scripts/check_secrets.py
cd backend; python -m evaluation.core_path_runner
```

## 计划自审

- 三个实现任务与已确认设计一一对应，未加入范围外重构。
- 每个任务都包含失败测试、安全反例、最小实现、focused verification、双重审查和独立提交。
- 远端验证严格绑定新 HEAD，基础 CI 与真实模型评测按依赖顺序执行。
- 计划明确禁止降低门槛、宽松放行派生字段或覆盖用户工作日志。
