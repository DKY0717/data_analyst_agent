# v0.3 语义层 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Data Analyst Agent 增加电商业务语义层，让 SQL 生成阶段不仅看到物理 Schema，还能理解销售额、退款率、复购率、客单价、地区、商品类别、月份等业务口径。

**Architecture:** 新增 `backend/app/semantic/`，用 YAML 定义指标、维度、别名、默认时间字段和 JOIN 关系，由 `SemanticLoader` 加载并格式化为 LLM prompt 可读摘要。`SQLGenerator` 在格式化 Schema 时合并语义摘要，`LLMService` prompt 明确要求优先遵循业务语义层，现有 AgentGraph 不新增节点。

**Tech Stack:** Python 3.10+、PyYAML、pytest、Qwen API prompt、DuckDB 业务 Schema。

---

## File Structure

```text
backend/
├── app/
│   ├── semantic/
│   │   ├── __init__.py
│   │   ├── ecommerce_metrics.yaml
│   │   └── semantic_loader.py
│   ├── agents/
│   │   └── sql_generator.py
│   └── services/
│       └── llm_service.py
└── tests/
    ├── test_semantic_loader.py
    ├── test_sql_generator.py
    └── test_llm_service.py
```

| 文件 | 职责 |
|---|---|
| `backend/app/semantic/ecommerce_metrics.yaml` | 机器可读业务语义配置，定义指标、维度、别名、JOIN 和默认项 |
| `backend/app/semantic/semantic_loader.py` | 加载 YAML，提供按别名查找、格式化 LLM 摘要等能力 |
| `backend/app/semantic/__init__.py` | 导出 `SemanticLoader` 和全局 `semantic_loader` |
| `backend/app/agents/sql_generator.py` | 将物理 Schema 和语义摘要合并后传给 LLM |
| `backend/app/services/llm_service.py` | 强化 SQL 生成 prompt，要求优先遵循业务语义层 |
| `backend/tests/test_semantic_loader.py` | 语义层核心单元测试 |
| `backend/tests/test_sql_generator.py` | 验证 SQLGenerator prompt 上下文包含语义摘要 |
| `backend/tests/test_llm_service.py` | 验证 LLM prompt 包含语义层约束 |

---

## Task 1: Add Semantic Config and Loader Tests

**Files:**
- Create: `backend/tests/test_semantic_loader.py`
- Create later: `backend/app/semantic/ecommerce_metrics.yaml`
- Create later: `backend/app/semantic/semantic_loader.py`
- Create later: `backend/app/semantic/__init__.py`

- [ ] **Step 1: Write failing tests**

Create `backend/tests/test_semantic_loader.py`:

```python
# 语义层测试
# 这些测试定义 v0.3 业务语义层的最小可用能力：指标、维度、别名和 LLM 摘要。

from app.semantic.semantic_loader import SemanticLoader


def test_find_metric_by_chinese_alias():
    """能通过中文别名找到销售额指标"""
    loader = SemanticLoader()

    metric = loader.find_metric("销售额")

    assert metric["key"] == "sales_amount"
    assert metric["expression"] == "SUM(orders.total_amount)"


def test_find_metric_by_business_alias():
    """能通过业务常用别名找到退款率指标"""
    loader = SemanticLoader()

    metric = loader.find_metric("售后率")

    assert metric["key"] == "refund_rate"
    assert "refunds" in metric["expression"]
    assert "orders.order_id = refunds.order_id" in metric["required_joins"]


def test_find_dimension_by_alias():
    """能通过别名找到地区维度和它需要的 JOIN 关系"""
    loader = SemanticLoader()

    dimension = loader.find_dimension("城市")

    assert dimension["key"] == "region"
    assert "regions.city" in dimension["fields"]
    assert "orders.customer_id = customers.customer_id" in dimension["required_joins"]


def test_defaults_include_time_field_and_limit():
    """默认配置要包含时间字段和返回上限，供 SQL 生成 prompt 使用"""
    loader = SemanticLoader()

    defaults = loader.get_defaults()

    assert defaults["time_field"] == "orders.order_date"
    assert defaults["limit"] == 1000


def test_format_for_prompt_contains_metrics_dimensions_and_defaults():
    """格式化摘要必须适合直接拼进 LLM prompt"""
    loader = SemanticLoader()

    summary = loader.format_for_prompt()

    assert "业务指标:" in summary
    assert "销售额 = SUM(orders.total_amount)" in summary
    assert "退款率 =" in summary
    assert "业务维度:" in summary
    assert "地区:" in summary
    assert "默认时间字段: orders.order_date" in summary
```

- [ ] **Step 2: Run tests to verify RED**

Run:

```bash
cd backend
pytest tests/test_semantic_loader.py -q
```

Expected: FAIL with `ModuleNotFoundError: No module named 'app.semantic'`.

---

## Task 2: Implement Semantic Config and Loader

**Files:**
- Create: `backend/app/semantic/__init__.py`
- Create: `backend/app/semantic/ecommerce_metrics.yaml`
- Create: `backend/app/semantic/semantic_loader.py`
- Test: `backend/tests/test_semantic_loader.py`

- [ ] **Step 1: Create semantic package init**

Create `backend/app/semantic/__init__.py`:

```python
"""业务语义层模块"""

from .semantic_loader import SemanticLoader, semantic_loader

__all__ = ["SemanticLoader", "semantic_loader"]
```

- [ ] **Step 2: Create ecommerce semantic YAML**

Create `backend/app/semantic/ecommerce_metrics.yaml`:

```yaml
metrics:
  sales_amount:
    name: 销售额
    aliases:
      - 销售额
      - GMV
      - 订单金额
      - 成交金额
    expression: SUM(orders.total_amount)
    default_table: orders
    description: 已下单订单的总金额，默认使用 orders.total_amount 汇总。

  order_count:
    name: 订单数
    aliases:
      - 订单数
      - 下单量
      - 订单量
    expression: COUNT(DISTINCT orders.order_id)
    default_table: orders
    description: 去重后的订单数量。

  customer_count:
    name: 客户数
    aliases:
      - 客户数
      - 用户数
      - 买家数
    expression: COUNT(DISTINCT customers.customer_id)
    default_table: customers
    description: 去重后的客户数量。

  average_order_value:
    name: 客单价
    aliases:
      - 客单价
      - 平均订单金额
      - AOV
    expression: SUM(orders.total_amount) * 1.0 / COUNT(DISTINCT orders.order_id)
    default_table: orders
    description: 销售额除以订单数。

  refund_rate:
    name: 退款率
    aliases:
      - 退款率
      - 售后率
      - 退单率
    expression: COUNT(DISTINCT refunds.refund_id) * 1.0 / COUNT(DISTINCT orders.order_id)
    default_table: orders
    required_joins:
      - orders.order_id = refunds.order_id
    description: 退款记录数除以订单数。

  repeat_purchase_rate:
    name: 复购率
    aliases:
      - 复购率
      - 重复购买率
    expression: COUNT(DISTINCT CASE WHEN customer_order_stats.order_count > 1 THEN customer_order_stats.customer_id END) * 1.0 / COUNT(DISTINCT customer_order_stats.customer_id)
    default_table: orders
    description: 下单次数超过 1 次的客户占全部下单客户的比例，生成 SQL 时可使用客户订单数子查询。

dimensions:
  month:
    name: 月份
    aliases:
      - 月份
      - 月
      - 按月
    fields:
      - strftime(orders.order_date, '%Y-%m')
    description: 默认按 orders.order_date 截取年月。

  region:
    name: 地区
    aliases:
      - 地区
      - 城市
      - 省份
      - 区域
    fields:
      - regions.region_name
      - regions.province
      - regions.city
    required_joins:
      - orders.customer_id = customers.customer_id
      - customers.region_id = regions.region_id
    description: 订单通过客户关联地区。

  category:
    name: 商品类别
    aliases:
      - 商品类别
      - 品类
      - 类目
      - 分类
    fields:
      - categories.category_name
    required_joins:
      - orders.order_id = order_items.order_id
      - order_items.product_id = products.product_id
      - products.category_id = categories.category_id
    description: 订单明细通过商品关联商品类别。

  payment_method:
    name: 支付方式
    aliases:
      - 支付方式
      - 支付渠道
    fields:
      - payments.payment_method
    required_joins:
      - orders.order_id = payments.order_id
    description: 订单通过支付表关联支付方式。

defaults:
  time_field: orders.order_date
  limit: 1000
  dialect: duckdb
```

- [ ] **Step 3: Implement semantic loader**

Create `backend/app/semantic/semantic_loader.py`:

```python
# 业务语义层加载器
# 将 YAML 中的电商指标和维度转换为 SQL Generator 可使用的上下文。

from pathlib import Path
from typing import Any, Dict, Optional

import yaml


class SemanticLoader:
    """加载并查询电商业务语义配置"""

    def __init__(self, config_path: str | Path | None = None):
        self.config_path = Path(config_path) if config_path else Path(__file__).parent / "ecommerce_metrics.yaml"
        self._data = self._load_config()

    def _load_config(self) -> Dict[str, Any]:
        """读取 YAML 配置；语义层是 SQL 生成的业务约束来源，因此缺失时应直接失败。"""
        with self.config_path.open("r", encoding="utf-8") as file:
            return yaml.safe_load(file) or {}

    def get_metrics(self) -> Dict[str, Dict[str, Any]]:
        """返回所有业务指标定义"""
        return self._data.get("metrics", {})

    def get_dimensions(self) -> Dict[str, Dict[str, Any]]:
        """返回所有业务维度定义"""
        return self._data.get("dimensions", {})

    def get_defaults(self) -> Dict[str, Any]:
        """返回默认时间字段、默认 LIMIT 和 SQL 方言"""
        return self._data.get("defaults", {})

    def find_metric(self, keyword: str) -> Optional[Dict[str, Any]]:
        """按指标 key、中文名称或别名查找指标"""
        return self._find_by_keyword(self.get_metrics(), keyword)

    def find_dimension(self, keyword: str) -> Optional[Dict[str, Any]]:
        """按维度 key、中文名称或别名查找维度"""
        return self._find_by_keyword(self.get_dimensions(), keyword)

    def _find_by_keyword(self, items: Dict[str, Dict[str, Any]], keyword: str) -> Optional[Dict[str, Any]]:
        """统一查找逻辑，返回时补充 key，方便调用方知道命中的配置项"""
        normalized_keyword = keyword.lower()
        for key, value in items.items():
            candidates = [key, value.get("name", ""), *value.get("aliases", [])]
            if any(str(candidate).lower() == normalized_keyword for candidate in candidates):
                return {"key": key, **value}
        return None

    def format_for_prompt(self) -> str:
        """格式化为 LLM 可读摘要，拼接到物理 Schema 后面"""
        lines: list[str] = []

        lines.append("业务指标:")
        for metric in self.get_metrics().values():
            lines.append(f"- {metric['name']} = {metric['expression']}")
            if metric.get("required_joins"):
                lines.append(f"  需要 JOIN: {'; '.join(metric['required_joins'])}")
            lines.append(f"  说明: {metric.get('description', '')}")

        lines.append("")
        lines.append("业务维度:")
        for dimension in self.get_dimensions().values():
            lines.append(f"- {dimension['name']}: {', '.join(dimension.get('fields', []))}")
            if dimension.get("required_joins"):
                lines.append(f"  需要 JOIN: {'; '.join(dimension['required_joins'])}")
            lines.append(f"  说明: {dimension.get('description', '')}")

        defaults = self.get_defaults()
        lines.append("")
        lines.append("默认规则:")
        lines.append(f"- 默认时间字段: {defaults.get('time_field')}")
        lines.append(f"- 默认返回上限: {defaults.get('limit')}")
        lines.append(f"- SQL 方言: {defaults.get('dialect')}")

        return "\n".join(lines)


# 全局语义层加载器，供 SQL Generator 复用
semantic_loader = SemanticLoader()
```

- [ ] **Step 4: Run semantic loader tests**

Run:

```bash
cd backend
pytest tests/test_semantic_loader.py -q
```

Expected: `5 passed`.

---

## Task 3: Connect Semantic Summary to SQL Generator

**Files:**
- Modify: `backend/app/agents/sql_generator.py`
- Test: `backend/tests/test_sql_generator.py`

- [ ] **Step 1: Write failing SQLGenerator test**

Add to `backend/tests/test_sql_generator.py`:

```python
def test_format_schema_includes_semantic_summary():
    """SQL Generator 给 LLM 的上下文必须包含业务语义层摘要"""
    generator = SQLGenerator()
    schema_context = {
        "tables": {
            "orders": {
                "table_name": "orders",
                "columns": [
                    {"name": "order_id", "type": "INTEGER", "nullable": False},
                    {"name": "total_amount", "type": "DECIMAL", "nullable": False},
                ],
                "primary_keys": ["order_id"],
            }
        }
    }

    result = generator._format_schema(schema_context)

    assert "物理数据库 Schema:" in result
    assert "业务语义层:" in result
    assert "销售额 = SUM(orders.total_amount)" in result
    assert "默认时间字段: orders.order_date" in result
```

- [ ] **Step 2: Run test to verify RED**

Run:

```bash
cd backend
pytest tests/test_sql_generator.py::test_format_schema_includes_semantic_summary -q
```

Expected: FAIL because `_format_schema()` does not include semantic summary yet.

- [ ] **Step 3: Update SQLGenerator**

Modify `backend/app/agents/sql_generator.py`:

```python
from ..semantic.semantic_loader import semantic_loader
```

Update `_format_schema()` near the end:

```python
        physical_schema = "\n".join(lines)
        semantic_summary = semantic_loader.format_for_prompt()

        return "\n".join([
            "物理数据库 Schema:",
            physical_schema,
            "",
            "业务语义层:",
            semantic_summary,
        ])
```

Keep the existing table formatting logic unchanged.

- [ ] **Step 4: Run SQLGenerator tests**

Run:

```bash
cd backend
pytest tests/test_sql_generator.py -q
```

Expected: all SQLGenerator tests pass.

---

## Task 4: Strengthen Qwen SQL Prompt for Semantic Layer

**Files:**
- Modify: `backend/app/services/llm_service.py`
- Test: `backend/tests/test_llm_service.py`

- [ ] **Step 1: Write failing prompt test**

Add to `backend/tests/test_llm_service.py`:

```python
class TestGenerateSQLPrompt:
    """测试 SQL 生成 prompt 是否明确使用业务语义层"""

    @pytest.mark.asyncio
    async def test_generate_sql_prompt_mentions_semantic_layer(self, client):
        captured_messages = {}

        async def fake_call_api(messages, temperature, max_tokens=2000):
            captured_messages["messages"] = messages
            return '{"sql": "SELECT SUM(total_amount) FROM orders", "tables": ["orders"], "explanation": "统计销售额"}'

        client._call_api = fake_call_api

        await client.generate_sql("统计销售额", "业务语义层:\n- 销售额 = SUM(orders.total_amount)")

        system_prompt = captured_messages["messages"][0]["content"]
        user_prompt = captured_messages["messages"][1]["content"]

        assert "优先遵循业务语义层" in system_prompt
        assert "业务指标口径" in system_prompt
        assert "业务语义层" in user_prompt
```

- [ ] **Step 2: Run test to verify RED**

Run:

```bash
cd backend
pytest tests/test_llm_service.py::TestGenerateSQLPrompt::test_generate_sql_prompt_mentions_semantic_layer -q
```

Expected: FAIL because current prompt does not mention semantic layer constraints.

- [ ] **Step 3: Update system prompt**

Modify `backend/app/services/llm_service.py` in `generate_sql()` system prompt:

```text
要求：
1. 只生成 SELECT 或 WITH 查询语句，禁止生成 DDL/DML 语句
2. 使用 DuckDB 方言（注意：DuckDB 不支持 MySQL 的 LIMIT offset, count 语法）
3. 优先遵循业务语义层中的业务指标口径、维度定义、默认时间字段和 JOIN 关系
4. 如果用户问题命中业务指标或维度别名，必须使用语义层给出的表达式和关联关系
5. 返回严格的 JSON 格式，不要包含任何其他文本
```

Also rename the user prompt label:

```text
数据库 Schema 与业务语义信息：
{schema_info}
```

- [ ] **Step 4: Run LLM service tests**

Run:

```bash
cd backend
pytest tests/test_llm_service.py -q
```

Expected: all LLM service tests pass.

---

## Task 5: Add Minimal End-to-End Semantic Prompt Test

**Files:**
- Modify: `backend/tests/test_sql_generator.py`

- [ ] **Step 1: Write integration-style test with mocked LLM**

Add to `backend/tests/test_sql_generator.py`:

```python
@pytest.mark.asyncio
async def test_generate_passes_semantic_context_to_llm():
    """生成 SQL 时传给 LLM 的 schema_info 应包含物理 Schema 和业务语义"""
    generator = SQLGenerator()
    schema_context = {
        "tables": {
            "orders": {
                "table_name": "orders",
                "columns": [
                    {"name": "order_id", "type": "INTEGER", "nullable": False},
                    {"name": "total_amount", "type": "DECIMAL", "nullable": False},
                    {"name": "order_date", "type": "DATE", "nullable": False},
                ],
                "primary_keys": ["order_id"],
            }
        }
    }

    captured = {}

    async def fake_generate_sql(question, schema_info):
        captured["question"] = question
        captured["schema_info"] = schema_info
        return {
            "sql": "SELECT SUM(orders.total_amount) AS sales_amount FROM orders",
            "tables": ["orders"],
            "explanation": "统计销售额",
        }

    original_generate_sql = llm_client.generate_sql
    llm_client.generate_sql = fake_generate_sql
    try:
        output = await generator.generate("统计销售额", schema_context)
    finally:
        llm_client.generate_sql = original_generate_sql

    assert output.sql == "SELECT SUM(orders.total_amount) AS sales_amount FROM orders"
    assert "表名: orders" in captured["schema_info"]
    assert "销售额 = SUM(orders.total_amount)" in captured["schema_info"]
```

- [ ] **Step 2: Run test**

Run:

```bash
cd backend
pytest tests/test_sql_generator.py::test_generate_passes_semantic_context_to_llm -q
```

Expected: PASS after Tasks 2-4.

---

## Task 6: Update Documentation and Work Diary

**Files:**
- Modify: `README.md`
- Modify: `docs/interview_guide.md`
- Modify: `logs/work-diary.md`

- [ ] **Step 1: Update README**

In `README.md` 项目亮点 section, add:

```markdown
- **业务语义层**：用 YAML 定义销售额、退款率、复购率、客单价等指标口径和地区、品类等维度，降低 LLM 猜字段和猜 JOIN 的风险。
```

In 核心架构 Mermaid, insert `Semantic Layer` before SQL Generator:

```mermaid
S --> Sem[Semantic Layer / 指标与维度]
Sem --> G[SQL Generator / Qwen]
```

- [ ] **Step 2: Update interview guide**

In `docs/interview_guide.md`, add one answer:

```markdown
### 8. 为什么要做业务语义层？

数据库 Schema 只能告诉模型有哪些表和字段，但不能告诉它“销售额”“退款率”“复购率”这些业务词应该如何计算。语义层把这些指标和维度定义成机器可读配置，SQL 生成时会把指标表达式、JOIN 关系和默认时间字段一起传给 LLM，降低字段幻觉和业务口径错误。
```

- [ ] **Step 3: Update work diary**

Add a concise entry to `logs/work-diary.md`:

```markdown
**v0.3 Phase 1: Schema 语义层** ✅
- 新增语义层配置和加载器。
- SQL Generator prompt 已合并物理 Schema 和业务语义摘要。
- Qwen SQL 生成 prompt 已明确要求优先遵循业务语义层。
- 验证：`pytest -q` 全部通过。
```

---

## Task 7: Final Verification

**Files:**
- All files touched in Tasks 1-6

- [ ] **Step 1: Run semantic focused tests**

Run:

```bash
cd backend
pytest tests/test_semantic_loader.py tests/test_sql_generator.py tests/test_llm_service.py -q
```

Expected: all focused tests pass.

- [ ] **Step 2: Run full backend test suite**

Run:

```bash
cd backend
pytest -q
```

Expected: all backend tests pass.

- [ ] **Step 3: Check whitespace**

Run:

```bash
git diff --check
```

Expected: no whitespace errors. Windows CRLF warnings are acceptable.

- [ ] **Step 4: Review final diff**

Run:

```bash
git status --short
git diff --stat
```

Expected: modified/new files match this plan. Pre-existing untracked files such as `AGENTS.md`, `.superpowers/`, `identifier.db`, and `scripts/` may remain unrelated.

---

## Self-Review

### Spec coverage

- v0.3 要求的 Schema 语义层：covered by Tasks 1-5.
- 指标定义、别名、维度、默认时间字段、JOIN 关系：covered by YAML and `SemanticLoader`.
- SQL Generator 接入语义层摘要：covered by Task 3.
- Prompt 明确遵循业务语义层：covered by Task 4.
- 测试覆盖：covered by Tasks 1, 3, 4, 5, and 7.

### Placeholder scan

No placeholder markers were found. Every implementation step includes concrete file paths, code snippets, commands, and expected outcomes.

### Type consistency

- `SemanticLoader.find_metric(keyword)` and `find_dimension(keyword)` both return `dict | None`.
- `format_for_prompt()` returns `str`.
- `SQLGenerator._format_schema(schema_context)` continues returning `str`, preserving existing callers.
- `llm_client.generate_sql(question, schema_info)` signature stays unchanged.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-03-v0.3-语义层实施计划.md`.

Two execution options:

**1. Subagent-Driven (recommended)** - dispatch a fresh subagent per task, review between tasks, fast iteration.

**2. Inline Execution** - execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?
