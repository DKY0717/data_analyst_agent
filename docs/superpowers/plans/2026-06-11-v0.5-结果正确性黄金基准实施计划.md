# v0.5 结果正确性黄金基准 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 使用 10 条人工审核参考 SQL 和固定业务断言，量化 Agent 最终分析结果的列结构、数值、排序和业务口径正确性。

**Architecture:** 新增独立黄金 case 文件、ReferenceQueryRunner、ResultComparator、ResultCorrectnessEvaluator 和 CorrectnessReportWriter。参考 SQL 必须经过现有 SQL Guard 后执行；比较器只处理结构化结果，不访问数据库或调用 Qwen；真实 Qwen 正确性评测接入手动 workflow，但第一版只记录基线，不阻塞普通 PR。

**Tech Stack:** Python 3.11、pytest、PyYAML、DuckDB、SQLGlot、GitHub Actions、Markdown/JSON

---

### Task 1: 建立 10 条黄金 case 与参考 SQL

**Files:**
- Create: `backend/evaluation/cases/golden_result_cases.yaml`
- Create: `backend/tests/test_golden_result_cases.py`

- [ ] **Step 1: 编写失败测试，定义黄金 case 文件结构**

测试要求：

```python
def test_golden_cases_have_exact_core_case_ids():
    cases = load_cases()
    assert [case["id"] for case in cases] == [
        "monthly_sales_2024",
        "top_products_by_sales",
        "customer_count_by_region",
        "sales_by_region",
        "sales_by_category",
        "refund_rate_by_category",
        "average_order_value_2024",
        "repeat_purchase_rate",
        "payment_method_sales",
        "monthly_order_count",
    ]


def test_each_golden_case_defines_reference_and_comparison():
    for case in load_cases():
        assert case["question"]
        assert case["category"] in {
            "aggregation", "time_series", "top_n", "dimension", "business_metric"
        }
        assert case["reference_sql"].strip()
        assert case["comparison"]["mode"] in {"unordered", "ordered", "top_n", "scalar"}
        assert case["comparison"]["required_columns"]
        assert case["comparison"]["absolute_tolerance"] >= 0
```

- [ ] **Step 2: 编写失败测试，要求关键 case 具有固定断言**

```python
def test_core_business_cases_have_fixed_assertions():
    cases = {case["id"]: case for case in load_cases()}
    required = {
        "monthly_sales_2024",
        "top_products_by_sales",
        "customer_count_by_region",
        "sales_by_category",
        "refund_rate_by_category",
        "average_order_value_2024",
        "repeat_purchase_rate",
        "monthly_order_count",
    }
    assert all(cases[case_id].get("fixed_assertions") for case_id in required)
```

- [ ] **Step 3: 运行测试并确认因 case 文件缺失失败**

Run:

```powershell
pytest backend/tests/test_golden_result_cases.py -q
```

Expected: FAIL，提示 `golden_result_cases.yaml` 不存在。

- [ ] **Step 4: 创建黄金 case 文件并写入人工审核参考 SQL**

参考 SQL 必须使用以下口径和稳定别名：

```sql
-- monthly_sales_2024 / ordered
SELECT strftime(order_date, '%Y-%m') AS month,
       SUM(total_amount) AS sales_amount
FROM orders
WHERE EXTRACT(YEAR FROM order_date) = 2024
GROUP BY month
ORDER BY month

-- top_products_by_sales / top_n
SELECT p.product_name,
       SUM(oi.quantity * oi.unit_price) AS sales_amount
FROM order_items oi
JOIN products p ON oi.product_id = p.product_id
GROUP BY p.product_id, p.product_name
ORDER BY sales_amount DESC, p.product_name
LIMIT 5

-- customer_count_by_region / unordered
SELECT r.region_name,
       COUNT(DISTINCT c.customer_id) AS customer_count
FROM customers c
JOIN regions r ON c.region_id = r.region_id
GROUP BY r.region_name

-- sales_by_region / unordered
SELECT r.region_name,
       SUM(o.total_amount) AS sales_amount
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
JOIN regions r ON c.region_id = r.region_id
WHERE EXTRACT(YEAR FROM o.order_date) = 2024
GROUP BY r.region_name

-- sales_by_category / unordered
SELECT c.category_name,
       SUM(oi.quantity * oi.unit_price) AS sales_amount
FROM order_items oi
JOIN products p ON oi.product_id = p.product_id
JOIN categories c ON p.category_id = c.category_id
GROUP BY c.category_name

-- refund_rate_by_category / unordered
SELECT c.category_name,
       COUNT(DISTINCT r.refund_id) * 1.0
         / NULLIF(COUNT(DISTINCT o.order_id), 0) AS refund_rate
FROM orders o
JOIN order_items oi ON o.order_id = oi.order_id
JOIN products p ON oi.product_id = p.product_id
JOIN categories c ON p.category_id = c.category_id
LEFT JOIN refunds r ON o.order_id = r.order_id
GROUP BY c.category_name

-- average_order_value_2024 / scalar
SELECT SUM(total_amount) * 1.0
         / COUNT(DISTINCT order_id) AS average_order_value
FROM orders
WHERE EXTRACT(YEAR FROM order_date) = 2024

-- repeat_purchase_rate / scalar
WITH customer_order_stats AS (
    SELECT customer_id, COUNT(DISTINCT order_id) AS order_count
    FROM orders
    GROUP BY customer_id
)
SELECT COUNT(DISTINCT CASE WHEN order_count > 1 THEN customer_id END) * 1.0
         / COUNT(DISTINCT customer_id) AS repeat_purchase_rate
FROM customer_order_stats

-- payment_method_sales / unordered
SELECT p.payment_method,
       SUM(o.total_amount) AS sales_amount
FROM orders o
JOIN payments p ON o.order_id = p.order_id
GROUP BY p.payment_method

-- monthly_order_count / ordered
SELECT strftime(order_date, '%Y-%m') AS month,
       COUNT(DISTINCT order_id) AS order_count
FROM orders
GROUP BY month
ORDER BY month
```

比较配置：

- 时间趋势使用 `ordered`，`order_by: [month]`。
- Top-N 使用 `top_n`，`order_by: [sales_amount, product_name]`。
- 客单价与复购率使用 `scalar`。
- 其余使用 `unordered`。
- 所有 case 使用 `absolute_tolerance: 0.001`。

- [ ] **Step 5: 使用固定种子数据库生成并人工核对固定断言**

先初始化当前项目数据：

```powershell
python -c "from pathlib import Path; import duckdb; c=duckdb.connect('data/database.duckdb'); c.execute(Path('database/init.sql').read_text(encoding='utf-8')); c.close()"
python database/seed_data.py
```

然后执行 10 条参考 SQL，将以下稳定断言写入对应 case：

- `monthly_sales_2024`: `row_count`
- `top_products_by_sales`: `row_count: 5`
- `customer_count_by_region`: `row_count`、`sum_column: {column: customer_count, value: 100}`
- `sales_by_category`: `row_count: 8`
- `refund_rate_by_category`: `row_count: 8`
- `average_order_value_2024`: `scalar: {column: average_order_value, value: 1956.7938144329896, absolute_tolerance: 0.001}`
- `repeat_purchase_rate`: `scalar: {column: repeat_purchase_rate, value: 0.76, absolute_tolerance: 0.001}`
- `monthly_order_count`: `row_count`、`sum_column: {column: order_count, value: 304}`

固定值只从固定随机种子数据库生成一次，写入后由测试长期锁定。

- [ ] **Step 6: 运行 case 结构测试**

Run:

```powershell
pytest backend/tests/test_golden_result_cases.py -q
```

Expected: PASS。

- [ ] **Step 7: 完成规格与质量审查并提交**

审查：

- 10 条 ID 与设计一致。
- 所有参考 SQL 使用 DuckDB 方言和稳定别名。
- 退款率、客单价和复购率符合语义层口径。
- case 中没有真实 Secret 或本地路径。

Commit:

```powershell
git add backend/evaluation/cases/golden_result_cases.yaml backend/tests/test_golden_result_cases.py
git diff --cached --check
git commit -m "test: add result correctness golden cases"
```

### Task 2: 实现 ReferenceQueryRunner

**Files:**
- Create: `backend/evaluation/reference_query_runner.py`
- Create: `backend/tests/test_reference_query_runner.py`

- [ ] **Step 1: 编写失败测试，定义安全参考查询结果**

```python
def test_reference_runner_validates_and_executes_safe_sql():
    guard = Mock()
    guard.validate.return_value = {
        "is_safe": True,
        "sanitized_sql": "SELECT 1 AS value LIMIT 1000",
        "reason": None,
    }
    query = Mock()
    query.execute.return_value = {
        "success": True,
        "columns": ["value"],
        "rows": [[1]],
        "row_count": 1,
    }

    result = ReferenceQueryRunner(guard=guard, query_runner=query).run("SELECT 1 AS value")

    assert result["guard_passed"] is True
    assert result["execution_success"] is True
    assert result["columns"] == ["value"]
    assert result["rows"] == [[1]]
    query.execute.assert_called_once_with("SELECT 1 AS value LIMIT 1000")
```

- [ ] **Step 2: 编写失败测试，验证 Guard 阻断和执行失败**

```python
def test_reference_runner_does_not_execute_blocked_sql():
    guard = Mock()
    guard.validate.return_value = {
        "is_safe": False,
        "sanitized_sql": "DROP TABLE orders",
        "reason": "禁止的语句类型",
    }
    query = Mock()

    result = ReferenceQueryRunner(guard=guard, query_runner=query).run("DROP TABLE orders")

    assert result["guard_passed"] is False
    assert result["execution_success"] is False
    assert result["error_type"] == "reference_guard_blocked"
    query.execute.assert_not_called()
```

执行失败返回 `error_type=reference_execution_failed`，不抛出异常。

- [ ] **Step 3: 运行测试并确认模块缺失**

Run:

```powershell
pytest backend/tests/test_reference_query_runner.py -q
```

Expected: FAIL。

- [ ] **Step 4: 实现 ReferenceQueryRunner**

接口：

```python
class ReferenceQueryRunner:
    def __init__(self, guard=sql_guard, query_runner=query_runner):
        ...

    def run(self, reference_sql: str) -> dict:
        ...
```

稳定输出字段：

```text
guard_passed
execution_success
columns
rows
row_count
sanitized_sql
error
error_type
```

关键要求：

- 先 Guard，再执行。
- 只使用 Guard 返回的 `sanitized_sql`。
- 不在日志中输出完整结果集。
- 捕获意外异常并返回 `reference_unexpected_error`。

- [ ] **Step 5: 运行定向测试与黄金 SQL smoke**

Run:

```powershell
pytest backend/tests/test_reference_query_runner.py -q
$env:PYTHONPATH='backend'
python -c "from evaluation.reference_query_runner import reference_query_runner; print(reference_query_runner.run('SELECT COUNT(*) AS count FROM orders'))"
```

Expected: PASS，smoke 返回成功结果。

- [ ] **Step 6: 审查并提交**

```powershell
git add backend/evaluation/reference_query_runner.py backend/tests/test_reference_query_runner.py
git diff --cached --check
git commit -m "feat: add guarded reference query runner"
```

### Task 3: 实现 ResultComparator

**Files:**
- Create: `backend/evaluation/result_comparator.py`
- Create: `backend/tests/test_result_comparator.py`

- [ ] **Step 1: 编写失败测试，定义值归一化与容差**

测试必须覆盖：

```python
def test_scalar_numbers_match_within_tolerance():
    result = comparator.compare(
        actual={"columns": ["rate"], "rows": [[Decimal("0.5004")]]},
        expected={"columns": ["rate"], "rows": [[0.5]]},
        comparison={"mode": "scalar", "required_columns": ["rate"], "absolute_tolerance": 0.001},
    )
    assert result["result_correct"] is True


def test_dates_are_compared_as_iso_strings():
    ...
```

- [ ] **Step 2: 编写失败测试，覆盖四种比较模式**

必须覆盖：

- `unordered`：行顺序不同仍正确。
- `ordered`：相同值但顺序不同，`order_matched=false`。
- `top_n`：行数、顺序和值都必须一致。
- `scalar`：缺少行或多行均失败。

- [ ] **Step 3: 编写失败测试，覆盖失败类型和差异样本限制**

```python
def test_comparator_reports_stable_failure_types_and_limits_samples():
    result = comparator.compare(...)

    assert result["failure_types"] == ["value_mismatch"]
    assert len(result["diff_samples"]) <= 5
```

同时覆盖：

- `column_mismatch`
- `row_count_mismatch`
- `value_mismatch`
- `order_mismatch`
- `fixed_assertion_failed`

- [ ] **Step 4: 运行测试并确认模块缺失**

Run:

```powershell
pytest backend/tests/test_result_comparator.py -q
```

Expected: FAIL。

- [ ] **Step 5: 实现 ResultComparator**

接口：

```python
class ResultComparator:
    def __init__(self, max_diff_samples: int = 5):
        ...

    def compare(
        self,
        actual: dict,
        expected: dict,
        comparison: dict,
        fixed_assertions: dict | None = None,
    ) -> dict:
        ...
```

输出：

```text
columns_matched
row_count_matched
values_matched
order_matched
fixed_assertions_matched
result_correct
failure_types
diff_samples
```

实现要求：

1. 列名大小写不敏感，但不进行语义猜测。
2. Decimal、float 和 int 使用绝对容差。
3. 日期/时间转 ISO 字符串。
4. `unordered` 使用归一化行的多重集合比较，保留重复行差异。
5. `ordered` 和 `top_n` 逐行比较。
6. `scalar` 要求一行。
7. 固定断言支持 `row_count`、`sum_column` 和 `scalar`。
8. 差异样本最多 5 条，不包含完整结果集。

- [ ] **Step 6: 运行比较器测试**

Run:

```powershell
pytest backend/tests/test_result_comparator.py -q
```

Expected: PASS。

- [ ] **Step 7: 审查并提交**

```powershell
git add backend/evaluation/result_comparator.py backend/tests/test_result_comparator.py
git diff --cached --check
git commit -m "feat: compare analytical query results"
```

### Task 4: 实现 ResultCorrectnessEvaluator

**Files:**
- Create: `backend/evaluation/result_correctness_evaluator.py`
- Create: `backend/tests/test_result_correctness_evaluator.py`

- [ ] **Step 1: 编写失败测试，定义单条正确 case 结果**

使用可注入 fake agent、reference runner 和 comparator：

```python
@pytest.mark.asyncio
async def test_evaluate_case_compares_agent_and_reference_results():
    result = await runner.evaluate_case(sample_case())

    assert result["case_id"] == "monthly_sales_2024"
    assert result["agent_execution_success"] is True
    assert result["reference_guard_passed"] is True
    assert result["reference_execution_success"] is True
    assert result["result_correct"] is True
```

- [ ] **Step 2: 编写失败测试，验证单条错误不中断批量评测**

覆盖：

- Agent 执行失败 -> `agent_execution_failed`
- Reference Guard 阻断 -> `reference_guard_blocked`
- Reference 执行失败 -> `reference_execution_failed`
- comparator 异常 -> `unexpected_error`
- 第一条失败后第二条仍执行

- [ ] **Step 3: 编写失败测试，定义汇总指标**

```python
assert summary["result_correctness_rate"] == 0.8
assert summary["column_match_rate"] == 0.9
assert summary["value_match_rate"] == 0.8
assert summary["order_match_rate"] == 0.75
assert summary["business_metric_accuracy"] == 0.75
assert summary["reference_guard_pass_rate"] == 1.0
assert summary["reference_execution_success_rate"] == 1.0
assert summary["fixed_assertion_pass_rate"] == 1.0
```

空结果返回全 0，不返回 NaN。

- [ ] **Step 4: 运行测试并确认模块缺失**

Run:

```powershell
pytest backend/tests/test_result_correctness_evaluator.py -q
```

Expected: FAIL。

- [ ] **Step 5: 实现 ResultCorrectnessEvaluator**

接口：

```python
class ResultCorrectnessEvaluator:
    def __init__(
        self,
        agent_runner=agent_graph.run,
        reference_runner=reference_query_runner,
        comparator=result_comparator,
        case_file=None,
    ):
        ...

    def load_cases(self) -> list[dict]:
        ...

    async def evaluate_case(self, case: dict) -> dict:
        ...

    async def evaluate_all(self) -> dict:
        ...

    def summarize_results(self, results: list[dict]) -> dict:
        ...
```

评测结果只能保存 Agent SQL、比较结果和有限差异，不保存 Agent 的完整大结果集。

- [ ] **Step 6: 实现 CLI 第一版**

支持：

```text
--case-file
```

CLI 执行完整评测并打印 summary JSON。退出码：

- 运行完成：`0`，即使真实正确率低于目标也先记录基线。
- case 文件或初始化错误：`2`。

Task 6 接入报告写入器后再打印报告路径。

- [ ] **Step 7: 运行评测器测试**

Run:

```powershell
pytest backend/tests/test_result_correctness_evaluator.py -q
```

Expected: PASS。

- [ ] **Step 8: 审查并提交**

```powershell
git add backend/evaluation/result_correctness_evaluator.py backend/tests/test_result_correctness_evaluator.py
git diff --cached --check
git commit -m "feat: evaluate analytical result correctness"
```

### Task 5: 验证参考 SQL 与固定断言

**Files:**
- Create: `backend/tests/test_golden_reference_results.py`

- [ ] **Step 1: 编写集成测试，逐条执行 10 条参考 SQL**

```python
def test_all_golden_reference_queries_are_safe_and_executable():
    for case in load_cases():
        result = reference_query_runner.run(case["reference_sql"])
        assert result["guard_passed"] is True, case["id"]
        assert result["execution_success"] is True, case["id"]
```

- [ ] **Step 2: 编写集成测试，固定断言全部通过**

使用 ResultComparator 对参考结果自身执行比较：

```python
comparison = result_comparator.compare(
    actual=reference_result,
    expected=reference_result,
    comparison=case["comparison"],
    fixed_assertions=case.get("fixed_assertions"),
)
assert comparison["fixed_assertions_matched"] is True, case["id"]
```

- [ ] **Step 3: 编写集成测试，参考 SQL 必须使用稳定标准列**

断言每条参考结果 columns 与 `comparison.required_columns` 一致，防止 YAML 声明与 SQL 别名漂移。

- [ ] **Step 4: 运行集成测试并修复黄金 case**

Run:

```powershell
pytest backend/tests/test_golden_result_cases.py backend/tests/test_golden_reference_results.py -q
```

Expected: 10 条参考 SQL、固定断言和列结构全部通过。

- [ ] **Step 5: 审查并提交**

```powershell
git add backend/tests/test_golden_reference_results.py backend/evaluation/cases/golden_result_cases.yaml
git diff --cached --check
git commit -m "test: verify golden reference results"
```

### Task 6: 输出中文结果正确性报告

**Files:**
- Create: `backend/evaluation/correctness_report_writer.py`
- Create: `backend/tests/test_correctness_report_writer.py`
- Modify: `backend/evaluation/result_correctness_evaluator.py`

- [ ] **Step 1: 编写失败测试，定义 Markdown 与 JSON 报告**

```python
writer = CorrectnessReportWriter(output_dir=tmp_path, timestamp="2026-06-11-180000")
paths = writer.write(sample_report())

assert paths["markdown"].name == "result-correctness-evaluation-2026-06-11-180000.md"
assert paths["json"].name == "result-correctness-evaluation-2026-06-11-180000.json"
```

Markdown 必须包含：

```text
# 结果正确性黄金基准报告
结果正确率
列结构匹配率
结果值匹配率
排序匹配率
核心业务指标命中率
参考 SQL Guard 通过率
固定断言通过率
失败 Case 明细
```

- [ ] **Step 2: 编写失败测试，验证报告只输出有限差异**

构造 10 条差异样本，断言 Markdown 最多展示 5 条，且不出现完整结果集字段。

- [ ] **Step 3: 实现 CorrectnessReportWriter**

行为沿用现有报告写入器：

1. 显式 `output_dir` 优先。
2. 其次读取 `EVALUATION_REPORT_DIR`。
3. 默认写入 `backend/evaluation/reports/`。
4. JSON 使用 UTF-8 和 `ensure_ascii=False`。
5. Markdown 展示失败类型、Agent SQL 和有限差异样本。

- [ ] **Step 4: 接入 ResultCorrectnessEvaluator CLI**

```python
report = await runner.evaluate_all()
paths = CorrectnessReportWriter().write(report)
print(json.dumps(report["summary"], ensure_ascii=False, indent=2))
print(f"Result correctness report: {paths['markdown']}")
```

- [ ] **Step 5: 运行报告测试**

Run:

```powershell
pytest backend/tests/test_result_correctness_evaluator.py backend/tests/test_correctness_report_writer.py -q
```

Expected: PASS。

- [ ] **Step 6: 审查并提交**

```powershell
git add backend/evaluation/correctness_report_writer.py backend/evaluation/result_correctness_evaluator.py backend/tests/test_correctness_report_writer.py
git diff --cached --check
git commit -m "feat: report analytical result correctness"
```

### Task 7: 接入手动真实 Qwen Workflow

**Files:**
- Modify: `.github/workflows/real-qwen-evaluation.yml`
- Modify: `backend/tests/test_workflow_files.py`

- [ ] **Step 1: 编写失败测试，要求真实 workflow 运行结果正确性评测**

```python
assert "python -m evaluation.result_correctness_evaluator" in commands
```

同时断言该步骤位于 NL2SQL 评测之后、质量门禁之前，并使用现有 `EVALUATION_REPORT_DIR`。

- [ ] **Step 2: 运行 workflow 测试并确认失败**

Run:

```powershell
pytest backend/tests/test_workflow_files.py -q
```

Expected: FAIL。

- [ ] **Step 3: 更新真实 Qwen workflow**

增加：

```yaml
- name: Run real result correctness evaluation
  env:
    QWEN_API_KEY: ${{ secrets.QWEN_API_KEY }}
  run: python -m evaluation.result_correctness_evaluator
```

第一版不把正确率加入 `quality_gate.py`，报告由现有 artifact 步骤统一上传。

- [ ] **Step 4: 运行 workflow 测试**

Run:

```powershell
pytest backend/tests/test_workflow_files.py -q
```

Expected: PASS。

- [ ] **Step 5: 审查并提交**

```powershell
git add .github/workflows/real-qwen-evaluation.yml backend/tests/test_workflow_files.py
git diff --cached --check
git commit -m "ci: add real result correctness evaluation"
```

### Task 8: 文档、真实 Qwen 基线与最终验收

**Files:**
- Create: `docs/data_analyst_agent_开发文档_v_0_5.md`
- Modify: `README.md`
- Modify: `docs/interview_guide.md`
- Modify: `logs/work-diary.md`
- Create: `backend/evaluation/reports/result-correctness-evaluation-*.json`
- Create: `backend/evaluation/reports/result-correctness-evaluation-*.md`

- [ ] **Step 1: 创建中文 v0.5 开发文档**

说明：

1. 为什么“SQL 执行成功”不等于结果正确。
2. 10 条黄金 case 的选择理由。
3. 参考 SQL、固定断言和四种比较模式。
4. ReferenceQueryRunner 的 SQL Guard 安全约束。
5. 正确性指标和失败类型。
6. 首轮真实 Qwen 失败 case 分析。
7. 如何使用正确性基线约束下一阶段 Schema Context Manager。

- [ ] **Step 2: 更新 README、面试稿和工作日记**

README 和面试稿增加：

- 结果正确性黄金基准。
- “能执行”和“算正确”的区别。
- 首轮真实 Qwen 正确率。
- 典型错误 JOIN、业务口径或排序失败案例。

- [ ] **Step 3: 运行完整确定性验证**

Run:

```powershell
pytest backend/tests -q
npm run build --prefix frontend
$env:PYTHONPATH='backend'
python -m evaluation.intent_evaluator
git ls-files -z | python scripts/check_secrets.py
git diff --check
```

Expected: 全部通过。

- [ ] **Step 4: 运行真实 Qwen Plus 结果正确性评测**

使用本地 `.env`，不得打印或提交 API Key：

```powershell
$env:PYTHONPATH='backend'
$env:QWEN_MODEL='qwen-plus'
python -m evaluation.result_correctness_evaluator
```

首轮最低目标：

- `result_correctness_rate >= 0.80`
- `column_match_rate >= 0.90`
- `business_metric_accuracy >= 0.75`
- `reference_guard_pass_rate = 1.0`
- `reference_execution_success_rate = 1.0`
- `fixed_assertion_pass_rate = 1.0`

若未达到目标，不放宽比较标准。根据失败 case 改进语义层或 SQL 生成 prompt，并补回归测试后重跑。

- [ ] **Step 5: 检查真实报告安全性**

Run:

```powershell
git add backend/evaluation/reports/result-correctness-evaluation-*.json backend/evaluation/reports/result-correctness-evaluation-*.md
git ls-files -z | python scripts/check_secrets.py
rg -n "Authorization: Bearer|QWEN_API_KEY=|sk-[A-Za-z0-9]" backend/evaluation/reports/result-correctness-evaluation-*.*
```

Expected: Secret Scan 通过，无真实敏感内容。

- [ ] **Step 6: 完成最终规格符合性审查**

确认：

- 10 条黄金 case 和设计一致。
- 参考 SQL 全部经过 SQL Guard。
- 参考 SQL、固定断言全部通过。
- 比较模式、容差和失败类型符合设计。
- 单条失败不会中断整批评测。
- 真实 Qwen 报告如实记录失败，不通过放宽标准提高分数。

- [ ] **Step 7: 完成最终代码质量审查**

检查：

- ResultComparator 无数据库、Qwen 或 Agent 依赖。
- Evaluator 与现有 NL2SQL Evaluation 职责分离。
- 报告不包含完整大结果集。
- 不提交 `.env`、DuckDB 文件、临时报告或无关文件。

- [ ] **Step 8: 最终提交**

```powershell
$json = Get-ChildItem 'backend/evaluation/reports/result-correctness-evaluation-*.json' | Sort-Object LastWriteTime | Select-Object -Last 1
$markdown = Get-ChildItem 'backend/evaluation/reports/result-correctness-evaluation-*.md' | Sort-Object LastWriteTime | Select-Object -Last 1
git add README.md docs/interview_guide.md docs/data_analyst_agent_开发文档_v_0_5.md logs/work-diary.md $json.FullName $markdown.FullName
git diff --cached --check
git commit -m "docs: complete v0.5 result correctness benchmark"
```
